apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "litecoin-chart.fullname" . }}-test-connection"
  labels:
    {{- include "litecoin-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "litecoin-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
